name: Verify VOSTD (macOS)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  verify-macos:
    runs-on: macos-14
    env:
      CARGO_TERM_COLOR: always
      VERUS_REPOSITORY: https://github.com/verus-lang/verus.git
      VERUS_BRANCH: irc11
      VERUS_PATCH: tools/patches/verus-irc11-vstd.patch

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Get Rust toolchain version
        id: rust-toolchain
        shell: bash
        run: |
          RUST_VERSION=$(grep 'channel' rust-toolchain.toml | sed -E 's/.*= *"(.*)"/\1/')
          if [ -z "$RUST_VERSION" ]; then
            echo "Failed to extract Rust version from rust-toolchain.toml"
            exit 1
          fi
          echo "RUST_VERSION=$RUST_VERSION" >> "$GITHUB_ENV"
          echo "Rust version: $RUST_VERSION"

      - name: Cache Rust toolchain
        uses: actions/cache@v6
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
            ~/.rustup/tmp
          key: ${{ runner.os }}-rust-toolchain-${{ env.RUST_VERSION }}

      - name: Cache Cargo dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Get Verus commit
        id: verus
        shell: bash
        run: |
          VERUS_COMMIT=$(git ls-remote "$VERUS_REPOSITORY" "refs/heads/$VERUS_BRANCH" | cut -f1)
          if [ -z "$VERUS_COMMIT" ]; then
            echo "Failed to resolve $VERUS_REPOSITORY branch $VERUS_BRANCH"
            exit 1
          fi
          echo "VERUS_COMMIT=$VERUS_COMMIT" >> "$GITHUB_ENV"
          echo "Using Verus $VERUS_BRANCH commit: $VERUS_COMMIT"
          DV_COMMIT=$(git rev-parse HEAD:dv)
          echo "DV_COMMIT=$DV_COMMIT" >> "$GITHUB_ENV"
          echo "Using dv commit: $DV_COMMIT"

      - name: Cache dv build artifacts
        uses: actions/cache@v6
        with:
          path: dv/target
          key: ${{ runner.os }}-dv-${{ env.DV_COMMIT }}

      - name: Cache Verus
        id: cache-verus
        uses: actions/cache@v6
        with:
          path: tools/verus
          key: ${{ runner.os }}-verus-${{ env.VERUS_BRANCH }}-${{ env.VERUS_COMMIT }}-${{ hashFiles('tools/patches/verus-irc11-vstd.patch') }}

      - name: Bootstrap Verus (if needed)
        shell: bash
        run: |
          if [ "${{ steps.cache-verus.outputs.cache-hit }}" = "true" ]; then
            echo "Using cached Verus"
          else
            echo "Cache miss, bootstrapping Verus $VERUS_BRANCH..."
            rm -rf tools/verus
            cargo dv bootstrap --upstream-verus --branch "$VERUS_BRANCH"
          fi
          test "$(git -C tools/verus rev-parse HEAD)" = "$VERUS_COMMIT"

      - name: Enable IRC11 alongside existing SC atomics
        shell: bash
        run: |
          if git -C tools/verus apply --reverse --check "$GITHUB_WORKSPACE/$VERUS_PATCH"; then
            echo "IRC11 compatibility patch is already applied"
          else
            git -C tools/verus apply "$GITHUB_WORKSPACE/$VERUS_PATCH"
          fi

      - name: Run verification
        shell: bash
        run: |
          set -o pipefail
          if ! make 2>&1; then
            echo "Verification failed"
            echo "VERIFY_FAILED=1" >> "$GITHUB_ENV"
            exit 1
          else
            echo "Verification passed"
          fi

      - name: Publish summary
        if: always()
        shell: bash
        run: |
          {
            echo "# macOS CI Summary"
            if [[ "${VERIFY_FAILED:-0}" == "1" ]]; then
              echo "- Verification: ❌ failed"
            else
              echo "- Verification: ✅ passed"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
